# refining-transcripts 2.1.1｜最小补丁验收

日期：2026-09-18。基准提交：`86ba015a16e67a03ef444e016a377b8584850cca`（2.1.0）。

本次仅补三处规则：说话人标签可靠性与源文件作用范围、待核标记保留可辨原文、多文件关系确认；修正T03的受限读取输入。没有新增Skill、工具依赖或处理框架。

## 本次执行结果

命令仍为 `python -m unittest discover -s tests -v`。先补检查，再修改规则与案例：

| 检查对象 | 实际结果 |
| --- | --- |
| 原版2.1.0与原检查 | 14项通过，退出码0 |
| 原版2.1.0与新增检查 | 18项中14项通过、4项失败，退出码1；缺少三处规则与T03输入隔离约定 |
| 补丁2.1.1 | 18项全部通过，退出码0 |
| 将“关键疑点已解决”改为“关键疑点不必解决” | 原版14项仍通过；新版18项中1项失败，退出码1 |
| 将“参考内容只用于理解，不再次输出”改为“参考内容用于理解，也再次输出到最终正文” | 原版14项仍通过；新版18项中1项失败，退出码1 |

两个反例各在独立临时副本中替换一次，再运行整套检查；正常版本保留原规则。新增断言仅拦截这两种已知文本反转，不是通用语义验证器；其他改写或矛盾仍可能漏检。新增的规则存在性检查同样不是行为测试。

T01—T08的独立模型行为验收仍未执行；T03仅修正测试输入约定，未搭建或运行受限读取宿主。本次没有验证实际长稿、Word输出或用户设备安装。下方2.1.0报告为历史记录，不代表本版另有行为验收结果。

---

# refining-transcripts 2.1.0｜验收报告

日期：2026-09-18。基准提交：`daffe93252c4e7d8f2d8e382cfaa9cfb5598d38a`（2.0.0）。验证环境：Python 3.13.5，标准库unittest，无需安装依赖。

## 已执行：静态文档回归

先编写14项静态检查和8个行为用例，再修改Skill与README。命令：

```bash
python -m unittest discover -s tests -v
```

| 阶段 | 实际结果 | 含义 |
| --- | --- | --- |
| 旧版2.0.0 + 新检查 | 14项中3项通过、11项失败，退出码1 | 新版需要的文档约束或结构尚未满足，不代表已观察到11个模型行为故障 |
| 修订后2.1.0 | 14项全部通过，退出码0 | 本次静态约束满足；不证明实际转写行为正确 |

旧版输出末尾：

```text
Ran 14 tests
FAILED (failures=11)
```

新版输出末尾：

```text
Ran 14 tests
OK
```

检查范围：元数据形状、Markdown标题/围栏/本地链接、分块主体与真实重复、超长案例与分段回读、覆盖记录、课程讨论保真、指定范围、四项交付状态、实体纠错、下游疑点继承、材料命令边界、旧有关键约束、8个案例结构、README版本与测试说明。

这些检查主要识别规定字段、关键措辞和已知歧义是否存在，不是完整的YAML/Agent Skills官方验证器，也不测量模型准确率。

## 已执行：差异与来源核对

修改前从GitHub连接读取基准文件，并核对本地副本的Git blob SHA：

| 文件 | 基准blob SHA |
| --- | --- |
| refining-transcripts/SKILL.md | `f2e35a2def359c8a9a66693c127f2d938bbc7818` |
| README.md | `c7639d198e75c3fb919ba01b78c54f19893d76c1` |

对照基准逐节检查，以下9节正文保持不变：核心定位、何时触发、硬规则、人名/术语/数字与说话人、时间戳与顺序、删除与保留、工作模式、Word排版规范、默认输出契约。证据优先级的顺序保持不变，仅补充识别纠错与现场事实的区别。

本次只修改SKILL.md和README.md，并新增静态检查、行为用例与本报告；不加入新的处理Skill、多Agent框架、自动上传或平台专属工具依赖。

## 未执行：独立模型行为验收

[8个行为验收用例](验收用例.md) 已准备，T01—T08均为“未执行”。本轮没有调用独立模型或宿主运行这些案例，也没有进行旧版/新版模型输出对照、多次重复采样或真实长课程测试。

因此未验证的项目包括：实际漏句/重出率、长案例连续性、小组讨论完整性、实际说话人纠错、下游不确定性保持，以及真实工具权限下是否拒绝材料命令。规则和用例写入不等于这些能力已经验证。

## 未执行：实际Word及安装验收

本轮修改的是Skill包，没有处理真实录音，也没有生成并检查逐字稿Word。没有在用户的Codex、WorkBuddy或其他设备安装、加载或测试此版本；仓库更新与本地安装是两回事。

## 结论

2.1.0的规则修订、README、静态检查和行为用例已完成静态核对。当前证据支持“文档静态检查通过”，不支持“所有模型行为通过”或“Word交付能力已实测通过”。
